services:
  mock:
    build: ./sl-linedata-mock
    ports:
      - "9080:80"
  service:
    build: ./transport-stop-counter-service
    ports:
      - "8080:8080"
  webapp:
    build: ./transport-stop-counter-webpage
    ports:
      - "3000:3000"
